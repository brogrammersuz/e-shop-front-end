import { FilerByAlphabeticOrderPipe } from './filer-by-alphabetic-order.pipe';

describe('FilerByAlphabeticOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new FilerByAlphabeticOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
